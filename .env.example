# Copy this file to .env and fill in your values, then run:
# docker run --rm --env-file .env -v ~/.aws:/root/.aws:ro aws-finops-dashboard:latest --all

# AWS authentication (choose either shared credentials via mounted ~/.aws, or set keys directly)
# AWS_ACCESS_KEY_ID=YOUR_KEY_ID
# AWS_SECRET_ACCESS_KEY=YOUR_SECRET
# AWS_SESSION_TOKEN=YOUR_SESSION_TOKEN
AWS_DEFAULT_REGION=us-east-1

# Optional: pick a specific profile from mounted ~/.aws files
# AWS_PROFILE=default

# Optional: Slack reporting
# SLACK_BOT_TOKEN=xoxb-your-token
